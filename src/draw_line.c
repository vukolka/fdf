#include <wchar.h>

int 	*get_rotation(void *v1)
{
	return (NULL);
}

void 	draw_line(void *mlx, void *win, void *v1, void *v2)
{
	int *from;
	int *to;

	from = get_rotation(v1);
	to = get_rotation(v2);
}